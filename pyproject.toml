[project]
name = "safety-manual"
version = "0.1.0"
description = "Generates an HTML site for the Online Safety manual"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.7.1",
]
